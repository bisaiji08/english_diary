<h1>Item Details</h1>
<p><strong>Name:</strong> <%= @item.name %></p>
<p><strong>Description:</strong> <%= @item.description %></p>
<p><strong>Price:</strong> <%= @item.price %> points</p>
<p>Your points: <%= current_user.tree.points %></p>

<% if current_user.tree.points >= @item.price %>
  <%= button_to "Buy", purchase_shop_path(@item), method: :post %>
<% else %>
  <p><em>You do not have enough points to buy this item.</em></p>
<% end %>

<%= link_to 'Back to Shop', shop_index_path %>
